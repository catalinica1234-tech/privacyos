import { Scale } from "lucide-react";
import { Card, Badge } from "@/components/ui";
export function RegulationCard({level="HIGH",title="Transparency",description="Evidence suggests that some privacy information may require contextual review.",evidence="Controlled demo evidence"}:{level?:"LOW"|"MEDIUM"|"HIGH"|"CRITICAL";title?:string;description?:string;evidence?:string}){const tone=level.toLowerCase() as "low"|"medium"|"high"|"critical";return <Card className="p-5"><div className="flex items-center justify-between gap-3"><div className="flex items-center gap-2 text-xs uppercase tracking-widest text-muted"><Scale className="h-4 w-4"/> Regulatory factor</div><Badge tone={tone}>{level}</Badge></div><h3 className="mt-5 text-base font-semibold">{title}</h3><p className="mt-2 text-sm leading-6 text-muted">{description}</p><div className="mt-5 rounded-xl surface-2 p-3 text-xs text-muted"><span className="font-medium text-[var(--privacy-text)]">Evidence:</span> {evidence}</div><p className="mt-4 text-xs font-medium text-privacy-cyan">Requires review →</p></Card>}
