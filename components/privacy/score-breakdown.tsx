import { Progress, Tooltip } from "@/components/ui";
export function ScoreBreakdown({ dimensions }: { dimensions: {label:string;value:number}[] }) { return <div className="space-y-4">{dimensions.map(d=><div key={d.label}><div className="mb-1.5 flex items-center justify-between gap-3"><div className="flex items-center gap-2"><span className="text-sm font-medium">{d.label}</span>{d.label==="Tracking"&&<Tooltip text="Measures observable tracking technologies in the evidence set."/>}</div><span className="text-sm font-semibold tabular-nums">{d.value}</span></div><Progress value={d.value}/></div>)}</div>; }
