"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, Globe2 } from "lucide-react";
import { Button, Card, Input, Select } from "@/components/ui";
export function ScannerForm(){const router=useRouter();const [url,setUrl]=useState("https://example.com");const [error,setError]=useState("");const start=()=>{try{const parsed=new URL(url);if(!["http:","https:"].includes(parsed.protocol))throw new Error();setError("");router.push(`/dashboard/scanner/progress?demo=1&url=${encodeURIComponent(parsed.toString())}`);}catch{setError("Enter a valid http(s) URL.");}};return <Card className="mx-auto max-w-3xl p-5 sm:p-8"><div className="mb-8 flex items-start gap-4"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl bg-privacy-cyan/10 text-privacy-cyan"><Globe2 className="h-6 w-6"/></div><div><h2 className="font-display text-xl font-semibold">Analyze a digital service</h2><p className="mt-1 text-sm text-muted">Configure a controlled demo analysis. Live scanning arrives in a later phase.</p></div></div><div className="space-y-5"><Input label="Website URL" value={url} onChange={(e: React.ChangeEvent<HTMLInputElement>)=>setUrl(e.target.value)} placeholder="https://example.com" error={error}/><Select label="Jurisdiction" defaultValue="CL"><option value="CL">🇨🇱 Chile — Ley 21.719</option><option value="EU">🇪🇺 European Union — GDPR</option><option value="BR">🇧🇷 Brazil — LGPD</option><option value="US-CA">🇺🇸 California — CCPA / CPRA</option></Select><div><p className="mb-2 text-sm font-medium">Analysis mode</p><div className="grid gap-3 sm:grid-cols-2"><label className="cursor-pointer rounded-xl border border-privacy-cyan/30 bg-privacy-cyan/5 p-4"><input type="radio" name="mode" defaultChecked className="accent-[#19D3C5]"/> <span className="ml-2 text-sm font-medium">Standard</span><p className="mt-1 pl-6 text-xs text-muted">Balanced demo workflow.</p></label><label className="cursor-pointer rounded-xl border border-subtle surface-2 p-4"><input type="radio" name="mode" className="accent-[#19D3C5]"/> <span className="ml-2 text-sm font-medium">Deep</span><p className="mt-1 pl-6 text-xs text-muted">Extended evidence preview.</p></label></div></div><Button onClick={start} size="lg" className="w-full sm:w-auto">Start demo analysis <ArrowRight className="h-4 w-4"/></Button></div></Card>}
