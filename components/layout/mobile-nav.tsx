"use client";
import Link from "next/link";
import { Menu, X, LayoutDashboard, FileSearch, ShieldCheck, BookOpen, Activity } from "lucide-react";
import { useState } from "react";
import { Logo } from "./logo";
export function MobileNav(){const [open,setOpen]=useState(false);const links=[['/dashboard','Overview',LayoutDashboard],['/dashboard/scanner','Scanner',FileSearch],['/dashboard/risks','Risks',ShieldCheck],['/dashboard/regulations','Regulatory',BookOpen],['/dashboard/rights','Rights',Activity]] as const;return <><header className="sticky top-0 z-40 flex h-16 items-center justify-between border-b border-subtle surface px-4 backdrop-blur-xl lg:hidden"><Logo href="/dashboard"/><button onClick={()=>setOpen(!open)} aria-label="Open navigation" className="rounded-xl border border-subtle surface-2 p-2">{open?<X className="h-5 w-5"/>:<Menu className="h-5 w-5"/>}</button></header>{open&&<div className="fixed inset-0 z-50 lg:hidden"><button aria-label="Close navigation" onClick={()=>setOpen(false)} className="absolute inset-0 bg-black/60 backdrop-blur-sm"/><div className="absolute right-0 top-0 h-full w-[86%] max-w-sm surface p-5 shadow-card"><div className="mb-8 flex items-center justify-between"><Logo href="/dashboard"/><button onClick={()=>setOpen(false)} className="rounded-xl surface-2 p-2"><X className="h-5 w-5"/></button></div><nav className="space-y-1">{links.map(([href,label,Icon])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="flex items-center gap-3 rounded-xl p-3 text-sm text-muted hover:surface-2 hover:text-[var(--privacy-text)]"><Icon className="h-5 w-5"/>{label}</Link>)}</nav><div className="mt-8 border-t border-subtle pt-5"><Link onClick={()=>setOpen(false)} href="/dashboard/settings" className="block rounded-xl p-3 text-sm text-muted">Settings</Link><Link onClick={()=>setOpen(false)} href="/dashboard/profile" className="block rounded-xl p-3 text-sm text-muted">Profile</Link></div></div></div>}</>}
