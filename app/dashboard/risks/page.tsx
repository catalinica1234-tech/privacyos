import { ShieldAlert } from "lucide-react";
import { AppShell } from "@/components/layout/app-shell";
import { Breadcrumbs } from "@/components/layout/breadcrumbs";
import { Alert, Badge } from "@/components/ui";
import { RiskCard } from "@/components/risks/risk-card";
import { demoAnalysis } from "@/lib/demo/data";
export default function RisksPage(){return <AppShell title="Risks"><div className="space-y-7"><Breadcrumbs items={[{label:"Dashboard",href:"/dashboard"},{label:"Risks"}]}/><div><Badge tone="info">DEMO INTELLIGENCE</Badge><h1 className="mt-3 font-display text-3xl font-semibold tracking-tight sm:text-4xl">Risk intelligence</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted">Potential risks are presented with severity, impact, likelihood and supporting context. They are not legal conclusions.</p></div><Alert tone="warning" title="Demo risks">These six risks are controlled examples used to demonstrate the interface and methodology workflow.</Alert><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Critical','1','critical'],['High','2','high'],['Medium','2','medium'],['Low','1','low']].map(([name,count,tone])=><div key={name} className="rounded-2xl border border-subtle surface p-5"><div className="flex items-center gap-2"><span className={`h-2 w-2 rounded-full ${tone==='critical'?'bg-risk-critical':tone==='high'?'bg-risk-high':tone==='medium'?'bg-risk-medium':'bg-risk-low'}`}/><span className="text-sm text-muted">{name}</span></div><p className="mt-4 font-display text-3xl font-semibold">{count}</p></div>)}</div><div className="grid gap-4 lg:grid-cols-2">{demoAnalysis.risks.map(r=><RiskCard key={r.id} risk={r}/>)}</div></div></AppShell>}
