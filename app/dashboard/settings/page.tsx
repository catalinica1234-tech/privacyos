"use client";
import { useState } from "react";
import { AppShell } from "@/components/layout/app-shell";
import { Card, Badge, Select, Alert } from "@/components/ui";
import { useTheme } from "@/components/providers";
export default function SettingsPage(){const {theme,setTheme}=useTheme();const [saved,setSaved]=useState(false);return <AppShell title="Settings"><div className="mx-auto max-w-3xl space-y-6"><div><Badge tone="info">FOUNDATION SETTINGS</Badge><h1 className="mt-3 font-display text-3xl font-semibold">Settings</h1><p className="mt-2 text-sm text-muted">Visual and demo preferences for this Foundation build.</p></div>{saved&&<Alert tone="success" title="Settings updated">Your local Foundation preferences were saved.</Alert>}<Card className="divide-y divide-black/5 dark:divide-white/5"><div className="p-6"><h2 className="font-display text-lg font-semibold">Appearance</h2><p className="mt-1 text-sm text-muted">Choose how PRIVACYOS looks on this device.</p><div className="mt-5 max-w-sm"><Select label="Theme" value={theme} onChange={(e: React.ChangeEvent<HTMLSelectElement>)=>{setTheme(e.target.value as "dark"|"light");setSaved(true)}}><option value="dark">Dark</option><option value="light">Light</option></Select></div></div><div className="p-6"><h2 className="font-display text-lg font-semibold">Language</h2><p className="mt-1 text-sm text-muted">Internationalization is prepared for a future release.</p><div className="mt-5 max-w-sm"><Select label="Language" defaultValue="en"><option value="en">English</option><option value="es">Spanish — coming later</option></Select></div></div><div className="p-6"><h2 className="font-display text-lg font-semibold">Notifications</h2><div className="mt-4 flex items-center justify-between rounded-xl surface-2 p-4"><div><p className="text-sm font-medium">Demo notifications</p><p className="mt-1 text-xs text-muted">No real alerts are sent in Foundation.</p></div><button className="h-6 w-11 rounded-full bg-privacy-cyan/30 p-1"><span className="block h-4 w-4 translate-x-5 rounded-full bg-privacy-cyan"/></button></div></div></Card></div></AppShell>}
