import { Globe2 } from "lucide-react";
import { AppShell } from "@/components/layout/app-shell";
import { Breadcrumbs } from "@/components/layout/breadcrumbs";
import { Alert, Badge, Card } from "@/components/ui";
import { JurisdictionCard } from "@/components/regulatory/jurisdiction-card";
import { RegulationCard } from "@/components/regulatory/regulation-card";
import { jurisdictions } from "@/lib/demo/data";
export default function RegulationsPage(){return <AppShell title="Regulatory Intelligence"><div className="space-y-7"><Breadcrumbs items={[{label:"Dashboard",href:"/dashboard"},{label:"Regulatory"}]}/><div><Badge tone="info">REGULATORY ENGINE · DEMO</Badge><h1 className="mt-3 font-display text-3xl font-semibold tracking-tight sm:text-4xl">Regulatory intelligence</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted">Review the jurisdiction layer independently from the UI. Specific legal rules will be connected in a later phase.</p></div><Alert title="Legal context">PRIVACYOS surfaces potential regulatory considerations. It does not determine whether an organization complies with law.</Alert><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{jurisdictions.map(j=><JurisdictionCard key={j.code} {...j} />)}</div><div className="grid gap-4 lg:grid-cols-2"><RegulationCard level="HIGH" title="Transparency" description="The demo scenario surfaces limited retention information as a factor requiring review."/><RegulationCard level="MEDIUM" title="Third-party disclosure" description="The demo scenario surfaces third-party signals that should be mapped to declared purposes."/></div><Card className="p-6"><div className="flex gap-3"><Globe2 className="h-5 w-5 text-privacy-cyan"/><div><h2 className="font-display text-lg font-semibold">Extensible by design</h2><p className="mt-2 text-sm leading-6 text-muted">Future jurisdictions such as PIPL, UK GDPR, Canada, India and Australia can be added as modular rule sets without changing the interface components.</p></div></div></Card></div></AppShell>}
